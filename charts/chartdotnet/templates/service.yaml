apiVersion: v1
kind: Service
metadata:
  name: {{ include "chartdotnet.fullname" . }}
  labels:
    {{- include "chartdotnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chartdotnet.selectorLabels" . | nindent 4 }}
